# Core dependencies for PBX system
PyYAML>=5.1

# FIPS-compliant encryption (Required for FIPS mode)
cryptography>=41.0.0

# Optional dependencies for enhanced features:
# For production SIP/RTP, consider:
# - pjsua (PJSIP Python bindings)
# - aiortc (WebRTC support)
# - twisted (Async networking)

# For enhanced audio processing:
# - pydub (Audio manipulation)
# - sounddevice (Audio I/O)

# For voice prompt generation (optional):
# System packages required: espeak, ffmpeg
#   sudo apt-get install espeak ffmpeg
# Python packages for cloud TTS (optional):
# - gTTS (Google Text-to-Speech, requires internet)
# - pyttsx3 (Offline TTS)
# - pydub (Audio conversion)

# For database backend:
# - sqlalchemy (ORM)
psycopg2-binary>=2.9.0  # PostgreSQL adapter for Python

# For enterprise integrations (Teams, Outlook, Zoom):
requests>=2.31.0  # HTTP library
msal>=1.24.0  # Microsoft Authentication Library

# For FIDO2/WebAuthn authentication
fido2>=1.1.0  # FIDO2/WebAuthn library for hardware security keys

# Audio Codec Support
# Opus codec - Modern, high-quality, low-latency audio codec
opuslib>=3.0.0  # Python bindings for Opus codec (requires libopus system library)

# iLBC codec - Internet Low Bitrate Codec with packet loss concealment
# Install: pip install pyilbc
# Note: May require system libraries on some platforms
# pyilbc>=1.0.0  # Uncomment to enable iLBC codec

# Speex codec - Open source speech codec from Xiph.Org
# Install: pip install speex
# Note: May require system libraries (libspeex) on some platforms  
# speex>=1.0.0  # Uncomment to enable Speex codec

# Voicemail transcription (FREE, offline speech recognition)
vosk>=0.3.45

# Machine Learning libraries (FREE, open source)
numpy>=1.24.0  # Array operations and numerical computing
scikit-learn>=1.3.0  # ML algorithms (classification, regression, clustering)
scipy>=1.11.0  # Scientific computing and optimization

# Alternative cloud-based transcription provider (optional):
# - google-cloud-speech (Google Cloud Speech-to-Text - requires paid API key)

# For web interface:
# - flask or fastapi (Web framework)
# - websockets (Real-time updates)

# For monitoring:
# - prometheus-client (Metrics)
# - redis (Caching/queues)
