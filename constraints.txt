# Constraints File for PBX System
#
# This file pins exact versions of all dependencies to ensure reproducible builds.
# It is used with uv pip install -c constraints.txt to lock dependency versions across
# different installation methods (uv pip install -e ., uv pip install -r requirements.txt, etc.)
#
# Usage:
#   uv pip install -c constraints.txt -e .
#   uv pip install -c constraints.txt -r requirements.txt
#   uv pip install -c constraints.txt -e ".[dev]"
#
# To update versions: Review and update version numbers based on the latest stable
# releases compatible with Python 3.13+. Test thoroughly after updating.
#
# Generated from: pyproject.toml
# Python Version: 3.13
# Last Updated: 2026-02-18

# ============================================================================
# Build Dependencies
# ============================================================================
# Note: Build dependencies (setuptools, wheel) are intentionally not pinned
# to allow flexibility across different build environments and tools.

# ============================================================================
# Core Dependencies
# ============================================================================
PyYAML==6.0.3
# cryptography is capped <49 by fido2 2.2.0 (requires cryptography>35,<49)
cryptography==48.0.1

# SIP/RTP Support
aiortc==1.14.0
twisted==26.4.0

# Audio Processing
pydub==0.25.1
sounddevice==0.5.5

# Voice Prompt Generation
gTTS==2.5.4
pyttsx3==2.99

# Database Backend
sqlalchemy==2.0.50
psycopg2-binary==2.9.12
alembic==1.18.4

# Enterprise Integrations
requests==2.34.2
msal==1.37.0

# Authentication
fido2==2.2.0

# Audio Codec Support
opuslib==3.0.1

# AI/ML Integration
vosk==0.3.45
spacy==3.8.14
nltk==3.9.4
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.17.1
pyAudioAnalysis==0.3.14
librosa==0.11.0
soundfile==0.14.0

# Video Codec Support
imageio-ffmpeg==0.6.0
# av is capped <17 by aiortc 1.14.0 (requires av>=14.0.0,<17.0.0)
av==16.1.0

# Data Validation
pydantic==2.13.4

# Web Interface
flask==3.1.3
werkzeug==3.1.8
jinja2==3.1.6
websockets==16.0

# Monitoring
prometheus-client==0.25.0
redis==8.0.0
psutil==7.2.2

# ============================================================================
# Development Dependencies
# ============================================================================

# Testing
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0
pytest-timeout==2.4.0
pytest-mock==3.15.1
pytest-xdist==3.8.0

# Code Quality
ruff==0.15.22
mypy==2.1.0

# Pre-commit Hooks
pre-commit==4.6.0

# Documentation
sphinx==9.1.0
sphinx-rtd-theme==3.1.0

# Security Auditing
pip-audit==2.10.1

# Development Utilities
ipython==9.14.1
ipdb==0.13.13

# ============================================================================
# Transitive Dependencies (commonly pinned for security/stability)
# ============================================================================
# These are dependencies of the above packages that are commonly pinned
# for security or stability reasons. Uncomment and pin if needed.
#
# certifi==2024.12.14
# charset-normalizer==3.4.1
# idna==3.10
# urllib3==2.3.0
# click==8.1.8
# markupsafe==3.0.2
# packaging==24.2
# typing-extensions==4.12.2
